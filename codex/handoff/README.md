# Handoff staging

Temporary staging directory for authoritative scientific handoff artifacts transferred into `fluxemu-standalone` for provenance verification and acceptance runs.

Do not unpack or modify source handoff archives manually unless the acceptance workflow explicitly requires it.
