# Real-model 50:50 U-13C glucose acceptance

> **Historical acceptance status.** In the original constrained local
> environment, both positive programme fixtures and the exact acetate-optimum
> negative oracle passed, while native FBA/FVA was ready for CI but could not be
> run locally. Current Stage 1 declares highspy as a base dependency, and the
> native FBA/FVA and sampled flux-to-MID paths run in green native CI.

The handoff archive was verified at SHA-256
`9acc0ed52cf6099e4f74760c20463d8a8d1973e6b213ab803916d76e070cef09`
and safely inspected from standalone starting commit
`c286a6adbde568daf20e7e3e96e0b4a8c1638654`. Its frozen prototype source is
`55b0bb73d2d90fa77599663140afd7f4f9a52f97`.

The persisted native flux model preserves 95 reactions, 72 metabolites, the
frozen common medium, glucose flux `-10`, and the biomass lower bound
`0.5243529041810849`. The focused test validates that reconstruction. In the
historical local checkout used for this record, native HiGHS FBA/FVA could not
be executed because highspy was then unavailable and installation was blocked
by the environment's package-index policy. The optimum numbers in this
manifest are therefore explicitly frozen references, not results generated by
that historical run. This does not describe the current dependency or CI
boundary: highspy is now installed by the standard package and exercised by
native Stage 1 CI.

The separately recorded **R1 12-target observation extension** verifies explicit
archived maps for Lactate, Citrate, Glycine, and Serine. All twelve requested
targets are therefore source-complete under the newly authorised target-only
semantics. Stage B2 itself remains unchanged.

The generic canonical engine now represents multiple isotope components driven
by a physical reaction, positive-part forward/reverse net-flux lifts, scaled
components, declared lump equalities, and explicit physical-direction coverage.
Physical turnover is evaluated from the actual complete net flux state rather
than inferred from bounds. The immutable 48-row Stage B2 projection manifest is
packaged separately from the frozen handoff.

The persisted builder constructs all 48 projections (49 isotope mapping records,
because the single GLUSy projection drives two independently mapped skeletons).
Both frozen optimum vectors reproduce all 48 frozen projected rates with a
maximum absolute residual of `1.7763568394002505e-15`.

The producer audit now uses the immutable class-restricted-FVA activity
certificate. `FRUpts2` retains permissive local bounds but is correctly excluded
because both certified directions are identically zero. Removing the certificate
restores the stricter bound-only failure.

The complete 12-target topology compiles. The frozen biomass state evaluates
twice identically and produces twelve normalized MIDs. CI generates its twelve
diagnostic PNGs as uncommitted workflow artifacts.
The frozen acetate state is an informative negative oracle at the whole-carbon four-atom layer containing
`oaa_c`, `mal__L_c`, `fum_c`, and `succ_c`: Malate, Fumarate, and Succinate have
zero physical turnover in that exact optimum. Their stationary MIDs are therefore
mathematically undefined without initial-pool labelling or an alternative flux
representative. The engine reports a singular layer (dimension 4, rank 1); no
tolerance was changed and no arbitrary MID was assigned. This historical GLPK
primal singularity is not an engine failure and is not a prerequisite for CI.
The workflow uses actual deterministic native HiGHS primals solely for the two
FBA/FVA programme-correctness gates; it does not require optimiser-selected
states to be suitable isotope fixtures or substitute either historical vector.

Programme acceptance separates flux analysis from EMU fixture suitability. The
positive acetate-class fixture is `theta1_sample_000`, selected deterministically
as the first stored Theta1 state whose complete twelve-target EMU evaluation is
well defined. It has acetate flux `11.84590102328278` and biomass flux
`0.5271467501821313`; it is a programme fixture, not a preferred biological
state. It evaluates twice identically and supplies the second twelve MIDs. CI
generates its twelve diagnostic PNGs, for 24 total positive-test plots, as
uncommitted workflow artifacts. The exact acetate optimum remains a required negative
regression and passes only when the singularity is detected without fabricated
MIDs, regularisation, or flux mutation.
